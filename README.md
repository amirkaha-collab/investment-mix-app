# Investment Mix App (Streamlit)

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy
Upload `app.py`, `requirements.txt`, and `data.xlsx` to your GitHub repo, then deploy on Streamlit Community Cloud.

- Main file path: `app.py`
- Branch: `main` (or whichever you use)

